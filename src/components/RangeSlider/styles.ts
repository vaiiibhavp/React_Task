import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    textTitle: {
        fontSize: 16,
        color: "#000",
        fontWeight: "700",
    },
    textSubtitle: {
        fontSize: 14,
        color: "#000",
        fontWeight: "400",
        marginTop: 2
    },
    viewRangePickerContainer: {
        position: "absolute",
        zIndex: 1,
        top: -13,
        left: 0,
        right: 0,
    }
})